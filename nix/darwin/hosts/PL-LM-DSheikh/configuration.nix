{ ... }:

# Per-host entrypoint for PL-LM-DSheikh.
# Shared concerns live in modules/ — add host-specific overrides here.

{
  # ── Host-specific overrides ───────────────────────────────────────────────
  # Add anything that applies only to this machine:
  # - additional hardware or macOS-specific modules
  # - per-host package additions
  # - host-specific services or settings
}
